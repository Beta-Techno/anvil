---
# Anvil provisioning playbook
# Currently: dev profile only. Add server, agent, user profiles later.
- hosts: localhost
  gather_facts: true
  become: false
  environment:
    DEBIAN_FRONTEND: noninteractive
  vars:
    ansible_python_interpreter: /usr/bin/python3
  roles:
    # Core system
    - { role: base, tags: [dev], become: true }
    - { role: security, tags: [dev], become: true }
    - { role: drivers, tags: [dev], become: true }
    - { role: observability, tags: [dev], become: true }

    # Docker
    - { role: docker, tags: [dev], become: true }

    # Networking
    - { role: tailscale, tags: [dev], become: true }

    # Git and dotfiles
    - { role: git_ssh, tags: [dev] }
    - { role: chezmoi_install, tags: [dev] }
    - { role: chezmoi_apply, tags: [dev] }

    # Shell configuration
    - { role: shell_config, tags: [dev] }

    # Build toolchain
    - { role: build_toolchain, tags: [dev], become: true }

    # Languages
    - { role: python, tags: [dev] }
    - { role: node, tags: [dev] }
    - { role: ruby, tags: [dev] }
    - { role: go, tags: [dev] }
    - { role: rust, tags: [dev] }
    - { role: dotnet, tags: [dev] }
    - { role: java, tags: [dev] }

    # Dev tools
    - { role: dev_tools, tags: [dev], become: true }

    # Secrets management
    - { role: lockbox, tags: [dev] }
    - { role: mani, tags: [dev] }

    # AI agents
    - { role: claude_cli, tags: [dev] }
    - { role: codex, tags: [dev] }

    # Editor/IDE
    - { role: lazyvim, tags: [dev] }
    - { role: zed, tags: [dev] }
    - { role: cursor, tags: [dev] }
    - { role: jetbrains_toolbox, tags: [dev] }

    # Desktop environment
    - { role: flatpak_snap, tags: [dev], become: true }
    - { role: fonts, tags: [dev] }
    - { role: terminals, tags: [dev], become: true }
    - { role: ghostty, tags: [dev], become: true }
    - { role: chrome, tags: [dev], become: true }
    - { role: docker_desktop, tags: [dev], become: true }
    - { role: nomachine, tags: [dev], become: true }
    - { role: terminal_extras, tags: [dev], become: true }
    - { role: activitywatch, tags: [dev] }
    - { role: flatpak_apps, tags: [dev], become: true }
    - { role: snap_apps, tags: [dev], become: true }

    # Cleanup
    - { role: cleanup, tags: [dev], become: true }
